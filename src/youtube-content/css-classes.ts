const prefix = 'hd';

export const hideThumbnailDuration = `${prefix}-thumbnail-duration`;
export const hideThumbnailProgress = `${prefix}-thumbnail-progress`;
export const hidePlayerOverlay = `${prefix}-player`;

export const thumbnailSelector = 'ytd-grid-video-renderer, ytd-video-renderer';
